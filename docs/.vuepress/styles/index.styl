@require './palette.styl';

html, body {
  background-color: $bgColor;
}

.navbar {
  background-color: $bgColor;

  .links {
    background-color: $bgColor;
  }
}

.sidebar {
  width $sidebarWidth
  background-color: $bgColor;
}

.page
  padding-left $sidebarWidth
